#include <iostream> // Library for output and input
using namespace std; // namespace setting
int main() {
    int x, y, z; // x, y, z is integer
    cin >> x >> y >> z; // read x, y, z
    cout << x * x + y - z / 2; // output x^2 + y - z : 2
    
    return 0; // program ended succesfully
}
